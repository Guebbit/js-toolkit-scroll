/**
 * TODO REDO
 * CREATED WITH CHATGPT
 */
import { setIntersection } from '../index';


describe("TODO (intersectionHelper) helper to automate Observer intersecate detection (when something is on screen most of the time)", () => {
	test("MANUAL CHECK via 'npm run serve', use /index.html and /tests/index.ts", () => {
		expect(setIntersection).toBeTruthy();
	});
});
